/**
 * Dependencies
 */

 const connection = require('./connection.js');

 module.exports = {
   /**
    * Selects all rows from table and returns data array of objects.
    *
    * @param {String}   table Name of MySQL table to query.
    * @param {Function} cb Callback function to return results.
    */
    
  }
